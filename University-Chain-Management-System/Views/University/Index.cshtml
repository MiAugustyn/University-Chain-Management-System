@model IEnumerable<University>
@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
    ViewData["Title"] = "Universities";
}

<div class="album py-5 bg-light">
    <div class="container">

        <div class="row">
            @foreach (University university in Model)
            {
                <div class="col-md-4">
                    <div class="card mb-4 box-shadow">
                        <div class="card-body">
                            <p class="card-text">@university.Name</p>
                            <p class="card-text">@university.City</p>
                            <div class="d-flex justify-content-between align-items-center">
                                <div class="btn-group">
                                    <a asp-controller="University" asp-action="Details" asp-route-id="@university.Id" type="button" class="btn btn-sm btn-outline-secondary">View</a>
                                    <button type="button" class="btn btn-sm btn-outline-secondary">Edit</button>
                                </div>
                                <small class="text-muted">@university.Id</small>
                            </div>
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>
