@model IEnumerable<Grade>
@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
    ViewData["Title"] = "Grades";
}

<div class="album py-5 bg-light">
    <div class="container">

        <div class="row">
            @foreach (Grade grade in Model)
            {
                <div class="col-md-4">
                    <div class="card mb-4 box-shadow">
                        <div class="card-body">
                            <p class="card-text">@grade.Value</p>
                            <p class="card-text">@grade.Student.FirstName @grade.Student.LastName</p>
                            <p class="card-text">@grade.Subject.Name</p>
                            <div class="d-flex justify-content-between align-items-center">
                                <div class="btn-group">
                                    <a asp-controller="Grade" asp-action="Details" asp-route-id="@grade.Id" type="button" class="btn btn-sm btn-outline-secondary">View</a>
                                    <a type="button" class="btn btn-sm btn-outline-secondary">Edit</a>
                                </div>
                                <small class="text-muted">@grade.Id</small>
                            </div>
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>
